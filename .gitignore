.DS_Store
.claude/settings.local.json
stocks/avg_q1fy26/
node_modules/
tools/.verify-output/

# Excel lock files
~$*.xlsx
~$*.xls

# Live brokerage exports — real account transaction data, never belongs in a
# public repo. folio2.html reads this off disk locally; it must stay untracked.
*.TRANSACTIONS.*.csv
